import { AdminPagePlaceholder } from "../../../_components/admin/AdminPagePlaceholder";

export default function StoreInfoPage() {
  return (
    <AdminPagePlaceholder
      title="기본 정보"
      description="매장 기본 정보 화면(읽기/수정 정책은 다음 단계에서 구현)"
    />
  );
}
